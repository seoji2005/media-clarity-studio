# REVIEW-002 — PR #1 (`941410c`) 독립 검토

| 항목 | 값 |
|---|---|
| **Reviewer** | **독립 Claude Code 리뷰 세션** (PR #1 작성자 세션이 아님 — [`../tasks/TASK-004.md`](../tasks/TASK-004.md) §0) |
| **대상 PR** | [seoji2005/media-clarity-studio#1](https://github.com/seoji2005/media-clarity-studio/pull/1) |
| **대상 HEAD (전체 SHA)** | `941410c2be2cba33c48d541415d24701b28bcf9a` |
| **비교 기준 (전체 SHA)** | `main` = `968a105c37a60331bd220ec0eb5b8b2075d20eee` |
| **대상 TASK** | TASK-002 (`0a7a1d6`) · TASK-007 (`941410c`) |
| **수행 TASK** | [`../tasks/TASK-004.md`](../tasks/TASK-004.md) |
| **날짜** | 2026-08-09 |
| **최종 판정** | **변경 요청 (REQUEST CHANGES)** |

**등급별 발견 수: 차단 0 · 중대 8 · 경미 6 · 의견 1**

> **판정은 병합이 아닙니다.** `AGENTS.md` R1 / ADR-0009에 따라 **병합은 사람 제품 오너만**
> 결정합니다. 이 리뷰는 PR #1의 상태·설명·Draft 여부를 **변경하지 않았고**, 대상 브랜치에
> **커밋하지 않았습니다** (§3.3).

---

## 1. 판정 근거 요약

`941410c`는 **차단 등급 결함이 없습니다.** 저장소 위생, 역사 기록 보존, U-08·U-11 분리,
REVIEW-001 F-01~F-22의 구조적 반영은 모두 실물로 확인되었습니다.

그러나 다음 두 축에서 **반드시 고쳐야 할 중대 결함 8건**이 남아 있어 `변경 요청`입니다.

1. **운영 구조 전환이 만든 새 규칙 자체가 실행 불가능하거나 판정 불가능합니다.**
   `941410c`가 신설한 `AGENTS.md` §3.2·§3.3·§3.4는 이 리뷰를 수행하는 과정에서
   **실제로 규칙 충돌을 일으켰습니다** (M-02·M-03·M-04). 특히 §3.3은 REVIEW-001 F-16이
   지적했던 "규칙을 지키면 규칙을 어기게 되는" 구조를 리뷰어 쪽에 다시 만들었습니다.
2. **REVIEW-001이 다음 리뷰에 넘긴 확인 요청 5건 중 4건이 "부분 해소"입니다.**
   구조는 생겼으나 (a) `ReferenceBundle`이 정의된 지표 하나를 뒷받침하지 못하고 (M-05),
   (b) 지표 계산 규약에 구현자 간 값이 갈리는 지점이 남아 있고 (M-06),
   (c) 방어 지표 G1–G4에 측정 범위·임계값이 없어 우회 경로가 열려 있습니다 (M-07).
   또한 F-02가 정정한 "승인됨 라벨의 정직성" 문제가 **새 ADR에서 재발**했습니다 (M-01).

---

## 2. 검증한 명령 / GitHub 근거

읽기 전용으로 원격 상태를 먼저 확인한 뒤 착수했습니다.

```bash
# GitHub (MCP, 읽기 전용)
pull_request_read(method="get",        owner=seoji2005, repo=media-clarity-studio, pullNumber=1)
pull_request_read(method="get_commits", owner=seoji2005, repo=media-clarity-studio, pullNumber=1)

# 로컬 (대상 HEAD를 fetch 후 검증)
git fetch origin claude/media-clarity-foundation-4fveu9 main
git rev-parse origin/claude/media-clarity-foundation-4fveu9   # -> 941410c2be2c...
git rev-parse origin/main                                     # -> 968a105c37a6...

git rev-list --count main..941410c                            # 커밋 수
git diff --numstat  main...941410c                            # 파일별 +/-
git diff --name-status main...941410c                         # 변경 유형
git ls-tree -r --name-only 941410c                            # 저장소 위생 (파일 전수)
git diff --numstat 0a7a1d6 941410c                            # 마지막 커밋 변경량
git merge-base --is-ancestor 0a7a1d6 941410c                  # force-push 여부
diff <(git show 968a105:README.md) <(git show 941410c:README.md | head -2)

# 문서 정합성 (전수)
grep -rn -i "codex" --include="*.md" .
grep -rhoE "U-[0-9]{2}"   --include="*.md" . | sort -u     # 참조
grep -rhoE "ADR-[0-9]{4}" --include="*.md" . | sort -u     # 참조
grep -oE "^### ADR-[0-9]{4}" docs/DECISIONS.md             # 정의
grep -rn -E "원본 복원|원본 복구|원본과 동일|복원 완료|정확히 복원|되찾았" --include="*.md" .
# 상대 링크 98개 전수 해석 (python, os.path.exists)
```

---

## 3. GitHub 상태 및 저장소 위생 검증표

| # | PR 설명의 주장 | 검증 방법 | 결과 |
|---|---|---|---|
| 1 | HEAD가 `941410c` | `pull_request_read` `head.sha`, `git rev-parse origin/…` | **일치** — `941410c2be2cba33c48d541415d24701b28bcf9a` |
| 2 | Open / Draft / 미병합 | PR API: `state=open`, `draft=true`, `merged=false` | **일치** |
| 3 | 커밋 3개 | `git rev-list --count main..941410c` = 3; API `commits=3` | **일치** (`38146ae` → `0a7a1d6` → `941410c`) |
| 4 | 변경 파일 14개 | `git diff --name-status main...941410c` = 14행; API `changed_files=14` | **일치** |
| 5 | `main` 대비 **+4,048 / −0** | `git diff --numstat` 합계 = 4048 / 0; API `additions=4048` | **일치** |
| 6 | 삭제 0의 이유가 `README.md` 2줄 보존 | `README.md` +171/−0, 나머지 13개는 신규 추가 | **일치** |
| 7 | `README.md` 최초 콘텐츠 보존 | `diff <(git show 968a105:README.md) <(git show 941410c:README.md \| head -2)` | **바이트 단위 동일** |
| 8 | 문서 전용 (코드·의존성·모델·CI·비밀정보 0건) | `git ls-tree -r --name-only 941410c` → **14개 전부 `.md`**. `.github/`·매니페스트·`.env` 없음 | **일치** |
| 9 | 추가된 줄에 비밀정보 없음 | 추가 라인 전수 패턴 검색 (`api key`/`secret`/`token`/`BEGIN`/`ghp_`/`AKIA`/`sk-…`) | **실제 비밀 0건** (일치어는 전부 "없음" 선언문 또는 `tokens[]` 계약 필드) |
| 10 | force-push 없음 | `git merge-base --is-ancestor 0a7a1d6 941410c` = true, `38146ae`도 조상 | **선형 이력 확인** |
| 11 | `U-XX` 유령 참조 0 | 참조 U-01~U-29 = 정의 U-01~U-29 | **일치, 유령 0** |
| 12 | `ADR-XXXX` 유령 참조 0 | 참조 ADR-0001~0027 = 정의 ADR-0001~0027 | **일치, 유령 0** |
| 13 | 깨진 상대 링크 0 | 14개 문서의 상대 링크 **98개** 전수 해석 | **깨진 링크 0** |
| 14 | `AGENTS.md` 내부 참조 실재 | R8–R10(L35–37), §3.1(L140), §3.2(L149), §3.3(L165), §3.4(L172), §3.5(L182) | **전부 실재** |
| 15 | 모든 TASK에서 Owner ≠ Reviewer | TASK-000/001/002/007 머리말 + `STATUS.md` §2.1·§2.2 | **일치** |
| 16 | 금지 용어 위반 0 | `원본 복원`류 전수 검색 | **위반 0** — 전부 §1 금지 규칙 정의 문맥 또는 비목표 서술 |
| 17 | **`941410c`가 `+514 / −150`** | `git diff --numstat 0a7a1d6 941410c` = **534 / 150** (11파일) | **불일치 — R-09 참조** |

> 17번을 제외한 **PR 설명의 모든 통계 주장이 실물과 일치**합니다. 17번은 경미하지만
> `AGENTS.md` R10("주장과 사실의 일치")의 적용 대상이므로 발견 사항으로 기록합니다.

---

## 4. 발견 사항

등급: `차단(Blocking)` / `중대(Major)` / `경미(Minor)` / `의견(Opinion)`

### 4.1 차단 (Blocking) — **0건**

없습니다. 저장소 위생·역사 기록 보존·U-08/U-11 분리·`README.md` 보존은 모두 실물로 확인되었습니다.

### 4.2 중대 (Major) — 8건

| ID | 등급 | 파일·절 | 문제 | 재현 / 논리적 근거 | 최소 수정 제안 |
|---|---|---|---|---|---|
| **M-01** | 중대 | `docs/DECISIONS.md` §1 (ADR-0019 L111, ADR-0022 L129, ADR-0024 L149, ADR-0025 L168, ADR-0026 L182) | **F-02가 정정한 문제가 새 ADR에서 재발.** 다섯 ADR이 **승인됨** 절에 있으나, 어느 것도 승인 근거를 기록하지 않았습니다. §상태 표기(L13)는 승인됨을 "사람 제품 오너가 승인했거나 프로젝트 전제로 주어진 것"으로 정의하고, L21은 "**에이전트가 스스로 만든 전략은 승인됨이 될 수 없다**"고 못박습니다 | 다섯 ADR은 전부 `0a7a1d6`(TASK-002)에서 **에이전트가 REVIEW-001에 대응해 새로 만든 전략**입니다. 대조군: ADR-0027만 "**왜 승인됨인가:** 사람 제품 오너의 지시"(L237)를 명시합니다. 나머지 다섯에는 그 문장이 없습니다. `CLAUDE.md` L36("내가 만든 전략을 승인됨으로 표시하지 않는다")과도 충돌합니다 | 각 ADR에 **승인 근거 한 줄**(누가·언제 승인했는가)을 추가하거나, 근거가 없으면 **제안됨** 절로 옮기십시오. ADR-0022는 "정책 승인됨"의 근거가 U-28(오너 미결)에 걸려 있으므로 특히 재검토가 필요합니다 |
| **M-02** | 중대 | `AGENTS.md` §3.3 (L167–169) vs §6.1 (L295–302) · §6.2 (L314) · §8(`STATUS.md` L199–204) | **F-16과 같은 종류의 규칙 충돌이 리뷰어 쪽에 새로 생겼습니다.** §3.3은 리뷰어의 "개입 수단은 PR 리뷰 코멘트와 자기 브랜치의 리뷰 문서(`docs/reviews/REVIEW-XXX.md`)**뿐**"이라고 **닫힌 목록**으로 규정합니다. 그런데 §6.2는 "새 작업은 반드시 TASK 파일을 먼저 만들고 시작"을 요구하고, §6.1·`STATUS.md` §8은 자기 TASK 행의 `STATUS.md` 갱신을 요구합니다 | **이 리뷰가 실제로 그 충돌에 걸렸습니다.** TASK-004를 수행하려면 `docs/tasks/TASK-004.md`(리뷰 문서 아님)와 `STATUS.md`(리뷰 문서 아님)를 만들어야 하는데, §3.3의 "뿐"은 둘 다 금지합니다. 규칙을 지키면 규칙을 어기게 됩니다 | §3.3의 목록에 **"자기 브랜치의 자기 TASK 파일"과 "`STATUS.md`의 §6.1 허용 범위"** 를 명시적으로 추가하십시오. 예: "…리뷰 문서, **자기 TASK 파일, §6.1이 허용하는 `STATUS.md` 기록**뿐" |
| **M-03** | 중대 | `AGENTS.md` §3.4 (L178) | **사실이 아닌 안전 주장.** "리뷰 세션은 리뷰 대상 브랜치에 쓰지 않으므로 **구조적으로 충돌하지 않습니다**"라고 단정합니다 | 거짓입니다. §6.1·§8이 리뷰 세션에게 `STATUS.md` 갱신을 **요구**하므로, 대상 브랜치(작성자 소유)와 리뷰 브랜치가 **같은 `STATUS.md`를 동시에 수정**합니다. 이 리뷰 브랜치가 그 상태입니다 — 리뷰 PR을 대상 브랜치에 병합할 때 `STATUS.md` 충돌이 발생할 수 있습니다. R9의 위험이 구조적으로 제거되지 않았습니다 | "구조적으로 충돌하지 않는다"를 삭제하고, **`STATUS.md`는 예외적으로 두 브랜치가 함께 만지는 파일임을 명시**한 뒤 충돌 해소 주체(병합하는 사람 오너)를 적으십시오 |
| **M-04** | 중대 | `AGENTS.md` §3.2 (L156–160) | **독립 리뷰 사용/생략 기준이 판정 불가능합니다.** 좌우 두 열의 조건이 **동시에 성립하는 변경**에 대해 우선순위가 없습니다 | 반례가 저장소 안에 있습니다. **TASK-002**는 우열 "이미 리뷰된 지적의 단순 반영"에 해당하면서, 동시에 좌열 "계약 변경"(`ReferenceBundle`·`ArtifactRef`·`Timebase` 신설)과 "핵심 알고리즘 변경"(지표 계산 규약 전면 재작성)에도 해당합니다. 두 사람이 정반대로 분류할 수 있습니다 | **"좌열 조건이 하나라도 성립하면 좌열이 이긴다"** 같은 tie-break 한 줄을 추가하고, 경계 사례로 TASK-002를 예시로 적으십시오 |
| **M-05** | 중대 | `docs/ARCHITECTURE.md` §3 (`ReferenceBundle/v1`, L209–212) vs `docs/EVALS.md` §5.1 (L433) | **`ReferenceBundle`이 정의된 지표 하나를 뒷받침하지 못합니다** (REVIEW-001 §4 확인 요청 3번). EVALS §5.1은 "**종류 정확도 — `degradation_kind` 혼동 행렬**"을 지표로 규정하는데, `ReferenceBundle.degradation_masks[]`의 필드는 `{ recipe_id, mask, severity }`뿐이고 **정답 `degradation_kind`가 없습니다** | 가설 쪽 `DegradedRegion/v1`에는 `degradation_kind`가 있습니다(§7.5 L568). 정답 쪽에 대응 필드가 없으므로 혼동 행렬의 정답 축을 만들 수 없습니다. `recipe_id`(예: `D-V3`) → `degradation_kind` 매핑은 **어느 문서에도 정의되어 있지 않습니다**. §3.1의 "없으면 미지원 보고" 규칙으로도 구제되지 않습니다 — 합성 열화 세트에서는 정답이 존재해야 하는데 계약이 담지 못하는 것이기 때문입니다 | `degradation_masks[]`에 **`degradation_kind` 필드를 추가**하거나, `recipe_id → degradation_kind` 매핑표를 EVALS §3.2에 명시하고 §5.1에서 참조하십시오 |
| **M-06** | 중대 | `docs/EVALS.md` §4.0(L253) · §4.1(L264,267) · §4.3(L311) · §4.4(L342) · §4.5(a)(L374) | **계산 규약에 구현자 간 값이 갈리는 지점이 남아 있습니다** (REVIEW-001 §4 확인 요청 2번 — "두 사람이 같은 숫자를 얻을 수 있는가"). 네 곳이 알고리즘·파생 규칙을 지정하지 않습니다 | ① **§4.1 제외 규칙**: "이어붙여 전체 문자열로 편집거리 계산" + "`inserted_spans` 가설 텍스트를 **분자에서 제외**"는 양립하지 않습니다. 전역 편집거리에서 특정 구간의 기여만 빼는 연산은 정의되지 않습니다. (a) 이어붙이기 **전에** 해당 가설 토큰을 제거, (b) 정렬 후 그 구간의 삽입 오류만 차감 — **두 해석의 값이 다릅니다.** ② **§4.3 매칭**: "IoU 최대화 1:1 매칭"만 있고 greedy인지 전역 최적 할당인지 없습니다. 같은 절의 §4.2는 "**모든 1:1 대응 중 총 편집거리 최소인 순열**"로 전역 최적을 명시했으므로 규약 밀도가 비대칭입니다. ③ **§4.4 귀속**: "가설 **문자**의 중앙 시각"을 요구하나, 어댑터가 단어 타이밍을 제공하지 않으면(§7.3.1) cue 시작·종료만 있습니다. cue 안에서 문자 시각을 어떻게 도출하는지(균등 배분?) 규정이 없습니다. ④ **§4.5(a)**: "문자 오프셋 `language_spans`를 **시간으로 투영**"의 투영 규칙이 없습니다. ⑤ **§4.0 토큰 단위**는 언어별로 **하나**를 고정하므로, 공백 구분 언어에서 **CER의 단위·분모가 정의되지 않습니다**(§4.1은 WER과 CER을 한 행으로 묶어 규정) | ①은 "이어붙이기 전에 제거"로 한 문장 고정, ②는 §4.2처럼 **전역 최적 할당(예: Hungarian)** 명시 + 동점 처리 규칙, ③은 **cue 내 균등 배분** 등 파생 규칙 명시(또는 단어 타이밍 없으면 cue 중앙 시각 사용), ④는 투영식 명시, ⑤는 §4.1을 WER 행과 CER 행으로 분리 |
| **M-07** | 중대 | `docs/EVALS.md` §5.4 방어 지표 G1–G4 (L494–501) · §12 (L726–737) | **방어 지표가 아직 집행 불가능하고, 우회 경로가 남아 있습니다** (REVIEW-001 §4 확인 요청 4번). (a) G1~G4의 **임계값이 전부 미정인데 U-XX로 등록되지 않았습니다.** (b) **측정 범위(전체 프레임 vs 열화 영역 내부)가 지정되지 않았습니다** | (a) "선명도 **하한**"(G1), "**하한** 미만"(G2), "**유의하게** 증가"(G3), "**축소**되면"(G4) — 전부 값이 없습니다. §12의 U-20은 "인공물 탐지 임계값, `T_px`·깨끗 영역 변경 상한"만 다루며 G1–G4를 포함하지 않습니다. 값을 모르는 것을 미해결로 등록하라는 R5가 여기서 지켜지지 않았습니다. (b) **자명한 해가 그대로 통과합니다**: 열화 영역이 화면의 5%일 때(§5.2 L457이 인정하는 전형적 비율) "**마스크 내부만 균일하게 흐리게**" 하는 해법은 전역 고주파 에너지를 거의 낮추지 않아 G1을 통과하고, 전역 프레임 간 변화량도 유지되어 G2–G4를 통과합니다. §5.2의 `metric@degraded`가 이를 잡지만 그것은 `clean_video`가 필요한 full-reference 지표이며, **실제 채점 입력에는 정답이 없습니다** — 즉 실입력에서는 방어가 없습니다 | G1–G4에 **`U-30`(또는 U-20 확장) 미해결 ID를 부여**하고, 각 지표에 **"열화 영역 내부에서 측정한다"는 범위 지정**을 추가하십시오. 실입력용으로는 §5.5 무참조 선명도를 **마스크 내부에서** 측정하는 규칙이 필요합니다 |
| **M-08** | 중대 | `docs/PRODUCT_SPEC.md` §6.1 N2 (L114) vs `docs/ARCHITECTURE.md` §5.2 (L318–320) · §5.1 P2 (L311) | **안전 보증의 과장 — 문서 간 모순.** N2는 "모자이크 처리된 인물의 신원 식별 … `ReconstructionPolicy`가 **기계적으로 차단**"이라고 단정합니다 | ARCHITECTURE §5.2는 정반대를 인정합니다: "`intentional_redaction`과 `generic_degradation`을 **자동으로 정확히 구분할 수 없습니다. 분류기는 틀립니다.**" 게다가 P2는 사용자 확인만 있으면 `allow`로 전환 가능하고, P4의 "신원 식별 **목적**으로는 지원하지 않는다"는 **의도**에 대한 규칙이라 기계가 판정할 수 없습니다. 기본 정책 수준 자체가 U-28로 미해결입니다. F-14("manifest가 재현성을 보장한다")와 **같은 종류의 거짓 보증**입니다 | N2를 "**기본값으로 차단하고, 해제하려면 사용자의 명시적 확인이 필요하다. 분류는 완전하지 않다(§5.2)**"로 완화하십시오. `PRODUCT_SPEC.md` §9의 "정직성" 품질 속성과도 그쪽이 일관됩니다 |

### 4.3 경미 (Minor) — 6건

| ID | 등급 | 파일·절 | 문제 | 근거 | 최소 수정 제안 |
|---|---|---|---|---|---|
| **R-09** | 경미 | PR #1 설명 §2 서문 | `941410c`의 변경량을 "**+514 / −150**(문서 10개 수정 + 1개 신규)"로 적었으나 실제는 **+534 / −150**입니다 | `git diff --numstat 0a7a1d6 941410c` 합계 = 534 / 150, 11파일(10 수정 + 1 신규). 파일 수는 맞고 삽입 줄 수만 20줄 틀립니다. `AGENTS.md` R10 "주장과 사실의 일치"의 적용 대상입니다 | PR 설명의 숫자를 `534`로 정정 (설명 수정 권한은 작성자 세션 또는 오너에게 있습니다 — 이 리뷰는 수정하지 않았습니다) |
| **R-10** | 경미 | `AGENTS.md` §6 작업 흐름 (L273–274) | 흐름의 첫 두 줄이 "GPT Work가 작업을 분해하고 프롬프트를 작성 → **TASK 파일 작성/확인**"이라 **TASK 파일을 GPT Work가 만드는 것처럼 읽힙니다** | §3(L127)과 ADR-0027은 GPT Work의 저장소 커밋을 "**하지 않음**"으로 규정합니다. 두 서술이 충돌하며, §3이 이기지만 흐름도만 읽는 독자는 반대로 이해합니다 | 해당 줄을 "**→ Owner(주 세션)가 TASK 파일 작성/확인**"으로 주체를 붙여 고치십시오 |
| **R-11** | 경미 | `AGENTS.md` §3.3 · §4 (리뷰 브랜치 규칙) | **리뷰 산출물이 저장소에 반영되는 경로가 정의되어 있지 않습니다.** 리뷰 브랜치를 만드는 규칙은 있으나, 그 브랜치의 PR을 **어디를 base로 열고, 누가 병합하며, 그 병합이 리뷰 대상 PR의 HEAD에 무엇을 하는지**가 없습니다 | §4 규칙 1은 "리뷰 브랜치는 리뷰 대상 브랜치에서 분기"만 규정합니다. 리뷰 PR을 대상 브랜치에 병합하면 대상 PR(#1)의 HEAD가 바뀌어 "리뷰어는 대상 브랜치를 수정하지 않는다"(§3.3)와 실질적으로 충돌합니다. 다음 리뷰 세션이 매번 이 판단을 다시 해야 합니다 | §4에 **리뷰 PR의 base·병합 주체·대상 PR HEAD에 미치는 영향** 세 줄을 추가하십시오 |
| **R-12** | 경미 | `README.md` L2 vs `AGENTS.md` §1 (L81–87) | 보존된 최초 2줄에 "**AI-assisted media restoration studio**"가 있어 §1의 용어 정책(적용 범위: 문서 전체)과 형식상 충돌하는데, **이것이 R2(기존 콘텐츠 보존)에 의한 의도적 예외라는 기록이 어디에도 없습니다** | R2는 삭제·축소를 금지하고, §1은 문서 전반에 재구성 용어를 요구합니다. 둘 다 "절대 어기면 안 되는 것"(§0)입니다. 다음 에이전트가 §1을 근거로 L2를 "고치면" **R2를 위반**합니다. GitHub 저장소 설명(description)도 같은 문구입니다 | `AGENTS.md` §1 또는 §0에 "**`README.md` 최초 2줄과 저장소 description은 R2에 따라 보존된 예외이며 수정하지 않는다**"는 한 줄을 추가하십시오 |
| **R-13** | 경미 | `docs/ARCHITECTURE.md` §3 (`utterances[].language` L190, `reference_cues[].language` L195) vs `language_spans[]` (L202–204) | 정답 계약 안에서 **언어가 두 곳에 이중으로 표현**되고, 문장 내 혼합 언어일 때 **어느 쪽이 정답인지 규정이 없습니다.** `utterances[].language`와 `reference_cues[].language`는 **단수 필드**입니다 | F-06이 요구한 "문장 내 언어 전환 표현"은 최상위 `language_spans[]`(시간 기반)로 해결되었으나, 단수 필드가 남아 있어 code-switching 발화에서 값이 상충합니다. `Transcript`(§7.3)는 `dominant_language`를 "**편의용 파생값일 뿐 정답이 아니다**"라고 명시했는데(L470), `ReferenceBundle`에는 그 문장이 없습니다 | `ReferenceBundle`에도 "**언어의 정답은 `language_spans[]`이며 `utterances[].language`·`reference_cues[].language`는 파생 편의 필드**"라는 한 줄을 추가하십시오 |
| **R-14** | 경미 | `docs/reviews/REVIEW-001.md` 머리말 (L5) vs §5 (L110) | 머리말의 "**대상 TASK: TASK-001**"이 모호합니다. §5는 "이 리뷰의 지적은 **TASK-000의 산출물** 전반에 반영되었습니다"라고 적습니다 | TASK-001은 이 리뷰를 **수행한** TASK이고, 검토 **대상**은 TASK-000의 산출물입니다. 한 칸이 두 뜻으로 읽힙니다. REVIEW-002는 이 혼동을 피하려고 "대상 TASK"와 "수행 TASK"를 분리했습니다 | 머리말을 "**수행 TASK: TASK-001 / 검토 대상: TASK-000 산출물(`38146ae`)**"로 분리하십시오 |

### 4.4 의견 (Opinion) — 1건

| ID | 파일·절 | 내용 |
|---|---|---|
| **O-01** | 저장소 전체 (3,436줄) · `STATUS.md` §7 R-9 | **문서 분량이 이미 위험 등록부의 R-9("문서가 커져 오너가 못 읽음")에 근접합니다.** 다만 그 대응책("오너용 진입점을 `README.md`와 `STATUS.md` §5로 한정")은 **실제로 작동합니다** — 이 리뷰가 확인한 바로는 두 문서(README 174줄, STATUS §5 32줄)만 읽어도 개발 경험이 적은 오너가 "지금 무엇이 막혀 있고 무엇을 답해야 하는가"(U-08·U-11)를 알 수 있습니다. 따라서 **지금 분할할 필요는 없다**고 봅니다. `STATUS.md` §6의 F6·F8(문서 분할 후속 작업)이 이미 등록되어 있으므로 그 시점까지 두는 것이 합리적입니다. **반영 의무 없음** |

---

## 5. REVIEW-001 F-01~F-22 상태표

분류: `해소` / `부분 해소` / `미해소` / `회귀` / `확인 불가`

| # | 등급(당시) | 상태 | 근거 (직접 확인한 위치) |
|---|---|---|---|
| F-01 | 차단 | **해소** | `PRODUCT_SPEC.md` §2 + AS-1~AS-4(L34–37), `PLAN.md` §0(L21–23)·§8(L203–205), `README.md` L46–55, `DECISIONS.md` §5 기각 목록(L453). "배점 가정으로 시각 재구성을 버릴 수 있다"로 읽히는 문장은 **전수 검색 결과 0건**. 범위 축소 권한은 일관되게 오너(AS-4) |
| F-02 | 차단 | **부분 해소** | ADR-0004(L250)·ADR-0018(L324)은 **제안됨**으로 정정 확인. 그러나 새 ADR-0019·0022·0024·0025·0026이 승인 근거 없이 **승인됨** 절에 배치 → **M-01** |
| F-03 | 차단 | **해소** | ADR-0024(L149–164), `EVALS.md` §2.5(L109–122), `DECISIONS.md` §4.1에서 U-06이 "**차단 아님**"(L410), `STATUS.md` §5(L140) |
| F-04 | 차단 | **부분 해소** | `ReferenceBundle/v1` 신설 확인(`ARCHITECTURE.md` §3). §4·§5의 지표를 하나씩 대조한 결과 **§5.1 종류 정확도만 정답 필드가 없습니다** → **M-05**. 나머지(cpWER·타임스탬프·무음·언어·탐지·full-reference·깨끗 영역)는 전부 대응 필드 존재 |
| F-05 | 차단 | **부분 해소** | `EVALS.md` §4가 지표별 `필요 정답/매칭/분모/제외/미지원` 표로 전면 재작성된 것 확인. 그러나 4~5개 지점에서 두 구현자가 다른 값을 얻습니다 → **M-06** |
| F-06 | 중대 | **해소** | `SpeechSegment.stream_id`·`concurrent_stream_ids`(§7.2), `Transcript…language_spans` 문자 오프셋(§7.3 L454–459), `SubtitleDocument.concurrent_cue_ids`(§7.4). 겹침·문장 내 전환 모두 표현 가능. 정답 쪽 단수 언어 필드 잔존은 경미 → **R-13** |
| F-07 | 중대 | **해소** | `AdapterCapabilityReport`와 능력별 fallback 표(`ARCHITECTURE.md` §7.3.1 L497–506) + "**없는 값을 기본값으로 채우는 것 금지**"(L505) |
| F-08 | 중대 | **해소** | `ArtifactRef/v1`·`Timebase/v1`·`TimeMapping/v1`·`RegionMask/v1`(§2.1–2.4), "`TimeMapping` 없는 시간 변경 열화는 평가 투입 불가"(L138), 이동 마스크 keyframe 규칙(L165–167) |
| F-09 | 중대 | **해소** | `EVALS.md` §2.1 dev/frozen-test, §2.2 출처·화자·장면·시드 누출 통제, §2.3 `n`·분석 단위·군집, §7 군집 부트스트랩·최소 실질 효과 |
| F-10 | 중대 | **해소** | §1.1 신호 6종(S1–S6) 표 + "S1 + 최소 하나의 다른 신호"(L37), ADR-0005 범위 정정(L60–62), `DECISIONS.md` §5 기각 목록(L456) |
| F-11 | 중대 | **부분 해소** | §5.1 탐지 지표·§5.3 깨끗 영역 보호·§5.4 G1–G4 전부 신설 확인. 그러나 G1–G4에 **임계값도 U-XX 등록도 측정 범위도 없어** 영역 국소 자명해가 통과합니다 → **M-07** |
| F-12 | 중대 | **해소** | §6.1 고정 시드 계층 무작위 추출 + "추출 결과를 먼저 기록한 뒤 시작, 교체 금지"(L553), §6.2 blind A/B, §6.3 거부권 |
| F-13 | 중대 | **해소** | `ReconstructionPolicy/v1`(§5) + P1–P5, A8, `PRODUCT_SPEC.md` N13, 사용자 흐름 §7.2의 "3번은 건너뛸 수 없습니다"(L190), `reconstruct(…, policy, …)`에서 policy가 필수 인자(L605). **다만 N2의 보증 표현이 과장** → **M-08** (게이트 자체의 존재는 해소) |
| F-14 | 중대 | **해소** | 재현성 T1/T2/T3(`ARCHITECTURE.md` §6), "manifest는 **T3만** 보증"(L337), `nondeterminism_sources`, ADR-0018 제안됨 + `DECISIONS.md` §5 기각 목록(L455) |
| F-15 | 중대 | **해소** | `AGENTS.md` §0.1 주체별 권한표(L43–49) + "R1의 목적은 검토 없는 자동 병합을 막는 것"(L51), ADR-0009 주체별 표(L96–99) |
| F-16 | 중대 | **부분 해소 (회귀 발생)** | 원 지적(§6 vs TASK-001 §4.2)은 §6.1 예외로 **해소 확인** (`TASK-001.md` L199–201에 정정 사유까지 기록). 그러나 `941410c`가 신설한 §3.3이 **리뷰어 쪽에 같은 종류의 충돌을 새로 만들었습니다** → **M-02**, 그리고 §3.4의 거짓 안전 주장 → **M-03** |
| F-17 | 중대 | **해소** | `TASK-000.md` 소급 기록 + `AGENTS.md` §6.2, "**이후 어떤 작업도 이 예외를 원용할 수 없다**"(L313, TASK-000 L36) |
| F-18 | 중대 | **해소** | ADR-0026, `AGENTS.md` §1 식별자 명명 규칙표(L91–96). 전수 검색 결과 `restore`/`RestoreAdapter`는 **금지 규칙을 정의하는 문맥에만** 등장. 모듈 ID 목록(`ARCHITECTURE.md` L276)도 `reconstruct` |
| F-19 | 중대 | **해소** | `PRODUCT_SPEC.md` §6.3이 근거를 범위·데이터·연산·일정·평가우선 5항목으로 재작성(L143–149) + "불가능이 아니라 미루는 것"(L151), `DECISIONS.md` §5 기각 목록(L454) |
| F-20 | 중대 | **해소** | ADR-0019(L111–125), 모델 비교표 필수 열 3종(L125), `PLAN.md` §6 각주(L177–179), `README.md` L124–126, U-05가 "라이선스 검토는 제외"로 축소(L390) |
| F-21 | 경미 | **해소** | ADR-0025, `ARCHITECTURE.md` §1 "A1의 정확한 의미" 절(L30–50) + 공유 자산표 + "**기반을 중복 구현하면 timebase가 갈라진다**"(L49–50) |
| F-22 | 경미 | **해소** | `AGENTS.md` §0.2 읽기 순서(L57–65), `README.md` L74, `CLAUDE.md` L7, `AGENTS.md` §10 우선순위와 읽기 순서를 혼동하지 말라는 주석(L406) |

**요약: 해소 17 · 부분 해소 5(F-02·F-04·F-05·F-11·F-16) · 미해소 0 · 회귀 0(단 F-16에 새 회귀성 결함 동반) · 확인 불가 0**

---

## 6. 운영 구조 전환 검증 결과

| # | 검증 항목 | 결과 |
|---|---|---|
| B-1 | 향후 역할·Owner/Reviewer·절차에서 Codex / Codex Cloud / `@codex review` 제거 | **충족.** `grep -rn -i "codex"` 전수 33건을 한 건씩 분류: **① 역사 기록**(STATUS §2.1·§3.2, TASK-000 머리말·L45, TASK-001 머리말·L17·L26·L31·L40, REVIEW-001 출처절, TASK-002 L14의 "이전 판에서 Codex였다", TASK-007 §5, DECISIONS ADR-0027 맥락) **② 향후 사용 금지를 규정하는 문맥**(AGENTS L14·L136–138, STATUS L91, TASK-007 §1·§2·§4·§7·§8, DECISIONS L217·L234). **향후 배정·절차 문맥 0건** |
| B-2 | 역사적 사실의 주체가 삭제·바꿔치기되지 않았는가 | **충족.** TASK-000 Reviewer(당시)=Codex, TASK-001 Owner(당시)=Codex / Reviewer(당시)=Claude, REVIEW-001 출처("Codex/GPT Work가 수행, 사람 오너 경유, Claude가 전사")가 **전부 원 주체 그대로 보존**. 삭제 대신 "**완료된 역사 기록 / 당시**" 표시로 분리. `main...941410c` diff의 삭제 줄 0을 감안하면 `0a7a1d6→941410c`의 −150줄이 유일한 삭제이며, 확인 결과 **주체 바꿔치기가 아니라 "미래 표준 절차"로 남아 있던 지시의 제거·교체**입니다 (예: `TASK-001.md` §6이 "당시 계획이며 사용되지 않았음 / 향후 표준 아님"으로 재작성) |
| B-3 | 역사 기록으로 표시된 문장이 미래 지시로 읽히지 않는가 | **충족(1건 유보).** TASK-000·TASK-001·REVIEW-001 모두 머리말에 "닫힌 역사 기록" 경고 + 현재 규칙 링크. 다만 `TASK-001.md` L23("아래 §2·§4·§7의 검토 항목과 태도 지침은 **여전히 유효한 참고 자료**")과 §8 머리말("이 목록은 향후 독립 리뷰 세션에도 그대로 유효")은 **역사 기록 문서 안의 현행 지시**입니다. 의도적이고 명시적이므로 결함으로 보지 않았으나, 경계가 문서 하나 안에서 두 번 뒤집힙니다 |
| B-4 | 네 역할의 권한이 서로 모순되지 않는가 | **1건 불충족.** GPT Work(커밋 안 함) / 주 세션(TASK Owner, 커밋) / 리뷰 세션(리뷰 문서만) / 일반 대화(커밋 안 함)는 AGENTS §3 · README · CLAUDE.md · PLAN §7 · DECISIONS ADR-0027 **다섯 곳에서 일관**합니다. 유일한 모순은 §6 작업 흐름이 TASK 파일 작성 주체를 GPT Work처럼 보이게 하는 것 → **R-10** |
| B-5 | 작성자·리뷰어 세션 분리와 자기 승인 금지가 모든 현행 TASK와 일치하는가 | **충족.** R8(L35)·§3.1(L145–147)·§6 흐름 주석(L285)·CLAUDE.md L21·L45·README L163에 일관. 현행 TASK-002·TASK-007의 Reviewer가 **"독립 Claude Code 리뷰 세션(미배정)"** 으로 기록되어 있고, 작성자 세션이 리뷰어로 적힌 곳은 **0건**. `STATUS.md` §2.2 보드와 TASK 파일 머리말도 일치 |
| B-6 | §3.2의 독립 리뷰 사용/생략 기준을 두 사람이 같은 결과로 판정할 수 있는가 | **불충족** → **M-04** |
| B-7 | 브랜치·TASK·리뷰 문서 절차가 실제 실행 가능한가 | **불충족.** 브랜치 규칙(§4)은 실행 가능하고 이 리뷰가 그대로 따랐습니다(`claude-review/task-004-pr-1-foundation`, 대상 HEAD에서 분기). 그러나 TASK 파일 생성과 `STATUS.md` 갱신이 §3.3의 닫힌 목록과 충돌하고(**M-02**), §3.4의 무충돌 주장이 사실이 아니며(**M-03**), 리뷰 산출물의 반영 경로가 미정의(**R-11**)입니다 |

### 6.1 U-08 · U-11 취급 검증 (C 항목)

| # | 항목 | 결과 |
|---|---|---|
| C-1 | 두 항목이 **Phase 1 계획 착수 차단**으로 유지되는가 | **충족.** `STATUS.md` §5(L127–132), `PLAN.md` §2 진입 조건표(L78), `DECISIONS.md` §4.1(L403–404, "차단 (Phase 1 계획 착수)"), `PRODUCT_SPEC.md` §11(L253·L256), `TASK-001.md` §8-7. 다섯 곳 전부 "Phase 1"로 범위 한정 |
| C-2 | 문서 전용 PR #1 병합을 **자동으로** 막는 문장이 남아 있는가 | **없음.** 반대 방향의 명시적 부정이 네 곳에 있습니다: `STATUS.md` L134–138, `PLAN.md` L81–83, `DECISIONS.md` L414–417, `TASK-001.md` L279. PR 설명의 "병합 전 확인 사항" 체크리스트에도 U-08·U-11이 **없습니다** |
| C-3 | PR 병합 영향이 **사람 오너의 별도 판단**으로 일관되게 기술되는가 | **충족.** 위 네 곳 모두 "PR #1의 병합 시점은 사람 제품 오너가 별도로 판단(ADR-0009)"으로 동일 문장. TASK-000 §4·TASK-002 §4·TASK-007 §7의 완료 조건 마지막 줄도 "사람 오너의 PR #1 병합 판단"으로 통일 |

### 6.2 문서 위생 (E 항목)

| # | 항목 | 결과 |
|---|---|---|
| E-1 | 유령 `U-XX` | **0건.** 참조 U-01~U-29 = 정의 U-01~U-29 (완전 일치, 번호 누락 없음) |
| E-2 | 유령 `ADR-XXXX` | **0건.** 참조 = 정의 = ADR-0001~0027 |
| E-3 | 깨진 상대 링크 | **0건** / 전체 98개 |
| E-4 | TASK 번호 중복·예약 충돌 | **없음.** 사용 = {000, 001, 002, 007}, `STATUS.md` §4 예약 = {003, 004, 005, 006}. TASK-007의 번호 선택 근거(§번호 선택 근거)가 `AGENTS.md` §6의 "예약 번호도 사용된 것으로 본다"와 일치. 이 리뷰는 **예약된 004를 그 용도 그대로** 사용 |
| E-5 | STATUS 보드 ↔ TASK 파일 Owner/Reviewer/Status | **일치.** 4개 TASK 전부 대조 완료 |
| E-6 | 금지된 "정확한 원본 복원" 주장 | **0건.** 검출된 7건은 전부 금지 규칙 정의(AGENTS §0 R3·§1) 또는 비목표·경계 서술(PRODUCT_SPEC N1·§8, ARCHITECTURE §7.5, EVALS §5.6). `README.md` L2의 "restoration"은 R2 보존 대상이나 예외 기록이 없음 → **R-12** |
| E-7 | 과제 필수 범위(자막·시각 재구성) 중 하나를 축소하는 서술 | **0건.** F-01 참조 |
| E-8 | 개발 경험이 적은 오너가 현재 상태·다음 판단을 이해할 수 있는가 | **가능.** `README.md` §"사람(제품 오너)이 할 일"(3항목) → `STATUS.md` §5(U-08·U-11 질문 2개 + 무엇이 막히는지)로 두 걸음에 도달. 두 문서 모두 "조사는 에이전트가 한다"(ADR-0024)를 명시 |
| E-9 | 문서 분량이 핵심 판단을 가리는가 | **가리지 않음** (오너 진입 경로가 짧게 유지됨). 다만 총 3,436줄로 R-9 위험에 근접 → **O-01** |

---

## 7. 확인하지 못한 항목 (정직하게)

| # | 확인하지 못한 것 | 이유 |
|---|---|---|
| 1 | **PR #1 작성 세션이 실제로 어떤 대화를 했는지** | 접근 불가이며 접근해서도 안 됩니다 (§3.1). 판단 근거는 저장소·TASK·diff·PR 상태뿐입니다 |
| 2 | **ADR-0019·0022·0024·0025·0026을 사람 오너가 실제로 승인했는지** | 저장소에 승인 기록이 없습니다. M-01은 "**승인 사실이 없다**"가 아니라 "**승인 근거가 기록되어 있지 않다**"는 지적입니다. 오너가 실제로 승인했다면 그 사실을 문서에 적으면 해소됩니다 |
| 3 | **ADR-0027이 정말 오너 지시인지** | 같은 이유. 다만 ADR-0027은 **근거를 명시**했으므로 M-01 대상에서 제외했습니다 |
| 4 | **§4·§5 지표를 실제로 구현했을 때 두 구현이 같은 값을 내는지** | 코드가 없습니다 (ADR-0001, Phase 0). M-06은 **규약 문언만으로 두 해석이 가능함**을 근거로 한 논리적 판정이며, 실측이 아닙니다 |
| 5 | **G1–G4가 막지 못하는 우회 해가 M-07에 적은 것 외에 더 있는지** | 실험 없이는 단정할 수 없습니다. M-07은 **문서에 적힌 정의만으로 도출 가능한 한 가지 경로**를 보인 것입니다 |
| 6 | **U-08·U-11·U-24·U-28의 답** | 사람 제품 오너만 답할 수 있습니다 (R5). 추측하지 않았습니다 |
| 7 | **README·STATUS의 이해 가능성에 대한 실제 오너의 반응** | 사람에게 물어보지 않았습니다. E-8은 **문서 경로의 길이와 명시성**에 근거한 판단입니다 |
| 8 | **`0a7a1d6` 이전 판 문서와의 문장 단위 대조 전수** | `0a7a1d6→941410c`의 −150줄은 전수 확인했으나, `38146ae→0a7a1d6`(전면 개정)은 항목 단위(F-01~F-22)로만 대조했고 문장 전수 대조는 하지 않았습니다 |
| 9 | **CI·시크릿 스캐닝 도구를 통한 검사** | 저장소에 CI가 없고(의도됨) 이 리뷰는 도구를 추가하지 않습니다. 위생 검증은 `git ls-tree` 전수 + 추가 라인 패턴 검색으로 했습니다 |

---

## 8. 결론 (사람 제품 오너용, 10줄 이내)

1. PR #1은 **문서만** 바꿉니다. 코드·설치·모델·CI·비밀번호 종류는 **한 건도 없습니다.** 직접 세어 확인했습니다.
2. PR 설명에 적힌 숫자(커밋 3개, 파일 14개, +4,048/−0)는 **한 곳(마지막 커밋 +514 → 실제 +534)만 빼고 전부 사실**이었습니다.
3. 처음 쓰신 README 두 줄은 **글자 하나 안 바뀌고 그대로** 남아 있습니다.
4. Codex를 앞으로 안 쓰기로 한 전환은 **제대로 되었습니다.** 과거에 있었던 일은 "옛날 기록"으로 표시만 하고 지우지 않았습니다.
5. "U-08(원어냐 번역이냐)·U-11(기한)"은 **Phase 1 착수만 막고 이 PR 병합을 막지 않는다**고 네 군데에 똑같이 적혀 있습니다.
6. 지난 리뷰의 지적 22건 중 **17건은 확실히 해결**, 5건은 **구조는 만들었으나 아직 덜 됐습니다.**
7. **당장 위험한 결함(차단)은 없습니다.** 그래서 "지금 병합하면 안 된다"는 뜻이 아닙니다.
8. 다만 **꼭 고쳐야 할 것 8건**이 있습니다. 크게 둘입니다 — (가) 이번에 새로 만든 리뷰 규칙이 실제로 지킬 수 없게 쓰여 있고, (나) 평가 지표 몇 곳이 사람마다 다른 숫자가 나오게 쓰여 있습니다.
9. 특히 **"승인됨"이라고 적힌 결정 5개는 누가 승인했는지 기록이 없습니다.** 오너께서 승인하신 적이 없다면 "제안됨"으로 내려야 합니다.
10. **병합 여부는 오직 오너의 결정입니다.** 이 리뷰는 PR #1을 건드리지 않았습니다.

---

## 9. 인계 메모 — 다음 담당자에게

**다음 담당자는 PR #1 작성 세션(주 세션)이거나 새 주 세션입니다. 리뷰 세션이 아닙니다.**

1. **이 리뷰는 대상 브랜치를 수정하지 않았습니다** (§3.3). 발견 사항 **전부 미수정 상태**입니다.
2. **차단 등급 0건**이므로 PR #1 설명의 "병합 전 확인 사항 — 차단 등급 발견 사항 해소" 항목은
   **해소할 대상이 없습니다.** 그 체크박스는 이 리뷰로 판정 가능해졌습니다.
3. **중대 8건은 새 TASK로 묶으십시오** (`STATUS.md` §4의 다음 미사용 번호 = **TASK-008**.
   003·004·005·006은 예약·사용 중이고 007은 사용됨 — `AGENTS.md` §6). 성격이 둘로 나뉩니다.
   - **운영 규칙 결함** (M-02·M-03·M-04·R-10·R-11): `AGENTS.md`만 고치면 됩니다. **먼저 하십시오** —
     이 결함들은 다음 TASK의 수행 방식 자체를 막습니다.
   - **명세 결함** (M-01·M-05·M-06·M-07·M-08·R-12·R-13): `DECISIONS.md`·`ARCHITECTURE.md`·
     `PRODUCT_SPEC.md`·`EVALS.md`. Phase 1a 착수 전에 해소되어야 지표 구현이 흔들리지 않습니다.
4. **M-01은 에이전트가 혼자 해결할 수 없습니다.** 승인 근거를 적으려면 오너에게 물어야 하고,
   물을 수 없으면 **제안됨으로 내리는 것이 R5에 맞는 처리**입니다. 추측으로 승인 근거를 지어내지 마십시오.
5. **M-07은 새 미해결 ID가 필요합니다.** 현재 최대는 U-29이므로 **U-30**이 다음 번호입니다.
   값을 정하지 말고 등록만 하십시오 (R5).
6. **R-09(PR 설명의 +514)는 이 리뷰가 고칠 수 없습니다.** PR 설명 수정 권한은
   작성자 세션 또는 오너에게 있습니다 (이 리뷰의 범위 밖 — `TASK-004.md` §2).
7. **이 리뷰 자체에는 Reviewer가 없습니다** (`TASK-004.md` "Reviewer가 없는 이유", §3.2).
   리뷰 결과를 재검토할 재귀적 독립 리뷰를 만들지 마십시오. **채택 여부는 사람 오너가 판단합니다.**
8. **확인하지 못한 9건**(§7)을 확인한 것으로 취급하지 마십시오. 특히 4·5번은 코드가 생긴 뒤
   **실측으로만** 닫을 수 있습니다.

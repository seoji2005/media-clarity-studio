# REVIEW-001 — TASK-001 저장소·아키텍처 독립 리뷰

| 항목 | 값 |
|---|---|
| **수행 TASK** | [`../tasks/TASK-001.md`](../tasks/TASK-001.md) — 이 리뷰를 **수행한** TASK |
| **검토 대상** | **TASK-000의 산출물** (`38146ae`) — 이 리뷰가 **검토한** 것 |
| **대상 커밋** | `38146ae` (PR #1, 브랜치 `claude/media-clarity-foundation-4fveu9`) |
| **판정** | **변경 요청 (REQUEST CHANGES)** |
| **날짜** | 2026-08-09 |
| **반영 상태** | 반영 완료 (같은 브랜치의 후속 커밋) |

## 출처 (Provenance)

> Independent review supplied by Codex/GPT Work through the human product owner;
> transcribed and addressed by Claude.

**이 기록의 성격에 대한 주의 — 완료된 역사 기록입니다**

- 원 리뷰는 **Codex/GPT Work**가 수행했고, **사람 제품 오너를 통해** 전달되었습니다.
  이것은 **실제로 일어난 일**이며, 사실이므로 바꾸지 않습니다.
- 이 파일은 Claude가 **전사(transcribe)** 한 것이며, 원문 그대로의 사본이 아닙니다.
  지적 사항을 항목화해 정리한 것입니다.
- **Claude는 자기 작업의 리뷰어가 아닙니다.** TASK-000의 당시 Owner는 Claude, 당시 Reviewer는 Codex이며,
  이 문서는 Reviewer의 판정을 Owner가 받아 적은 기록입니다.
- 따라서 이 파일의 "반영함" 표시는 **Owner의 주장**입니다. 검증은 Reviewer의 몫입니다.

> **향후 리뷰의 주체는 다릅니다.**
> 운영 구조 전환(TASK-007, ADR-0027) 이후 **독립 리뷰는 새 독립 Claude Code 세션**이 수행합니다.
> 위 출처 기술은 **이 리뷰 1건의 역사 기록**이며, 앞으로의 리뷰 배정 근거가 아닙니다.
> 현재 규칙: [`../../AGENTS.md`](../../AGENTS.md) §3·§9.

---

## 1. 판정 요약

`38146ae`의 문서 기반은 구조와 위생 면에서는 통과했으나,
**검증되지 않은 가정에 근거한 범위 축소**, **승인되지 않은 전략의 승인 표시**,
**계산 불가능한 지표 정의**, **순환 의존**, **거버넌스 자기모순**이 확인되어
`변경 요청` 판정을 받았습니다.

핵심 지적을 한 줄로 요약하면:

> "문서가 정직해 보이지만, 정직하지 않은 지점이 몇 군데 있다.
> 특히 모르는 것(배점)을 근거로 필수 산출물(시각 재구성)을 버릴 수 있게 적어 두었다."

---

## 2. 발견 사항

등급: `차단(Blocking)` / `중대(Major)` / `경미(Minor)` / `의견(Opinion)`

| # | 등급 | 위치 | 문제 | 반영 |
|---|---|---|---|---|
| F-01 | 차단 | `PLAN.md`, `PRODUCT_SPEC.md`, ADR-0004 | 과제 범위와 상용 MVP를 섞어, **검증되지 않은 배점 가정**("자막이 채점 중심")으로 시각 재구성을 폐기 가능한 것처럼 기술 | `PRODUCT_SPEC.md` §2 신설 (AS-1~AS-4), `PLAN.md` §0, ADR-0004 재진술 |
| F-02 | 차단 | `DECISIONS.md` | 에이전트가 스스로 만든 전략(ADR-0004)을 **승인됨**으로 표시. ADR-0018도 근거 없이 승인됨 | 두 항목을 **제안됨**으로 정정, 정정 사유를 문서에 명시 |
| F-03 | 차단 | `EVALS.md` §2.2, `DECISIONS.md` U-06 | **순환 의존** — 개발 경험이 적은 오너에게 코퍼스 *조사*를 요구하면서, 그 답 없이는 Phase 1이 막힌다고 기술 | ADR-0024 신설(조사=에이전트 / 승인=사람), U-06을 차단 목록에서 해제 |
| F-04 | 차단 | `EVALS.md` 전반 | 여러 지표가 "정답"을 전제하나 **정답의 형태가 정의되지 않음.** cpWER·언어 지표·시각 지표가 계산 가능한지 판단 불가 | `ARCHITECTURE.md` §3 `ReferenceBundle/v1` 신설 (ADR-0020) |
| F-05 | 차단 | `EVALS.md` §3 | 지표 이름만 있고 **계산 규약 없음** — 매칭 규칙·분모·제외·미지원 조건 미정의. 구현자마다 다른 숫자가 나옴 | `EVALS.md` §4 전면 재작성 (지표별 규약표) |
| F-06 | 중대 | `ARCHITECTURE.md` | `SpeechSegment`·`Transcript`·`SubtitleDocument`가 **겹치는 발화와 문장 내 언어 전환을 표현할 수 없음.** 입력의 핵심 난점을 계약이 못 담음 | 다중 스트림(`stream_id`) + 문자 오프셋 `language_spans`로 개정 |
| F-07 | 중대 | `ARCHITECTURE.md` | 어댑터가 단어 타이밍·신뢰도·LID를 제공하지 않는 경우의 **대체 동작 미정의.** 없는 값을 기본값으로 채우면 지표가 조용히 오염됨 | `AdapterCapabilityReport` + 능력별 fallback 표 신설 (§7.3.1) |
| F-08 | 중대 | `ARCHITECTURE.md` | **공통 산출물 참조·시간축 계약 부재.** 무음 삽입·프레임 드롭 시 원본↔열화 시간 대응이 없어 타임스탬프 지표가 틀림. 이동하는 모자이크 영역도 표현 불가 | `ArtifactRef`·`Timebase`·`TimeMapping`·`RegionMask` 신설 (ADR-0021) |
| F-09 | 중대 | `EVALS.md` | **dev/test 분리·누출 통제·표본 수·불확실성 보고 부재.** 우연을 개선으로 오인할 구조 | §2(분할·누출·표본) §7(짝지은 비교·최소 실질 효과) 신설 (ADR-0023) |
| F-10 | 중대 | `EVALS.md` §1 | 합성 열화를 **"유일하게 신뢰 가능한 신호"** 로 규정 → 그 지표에 과적합 유도 | §1.1 신호 6종(S1–S6) 표로 대체, ADR-0005 범위 정정 |
| F-11 | 중대 | `EVALS.md` §4 | 시각 평가에 **열화 영역 탐지 지표 없음**, 깨끗 영역 훼손 지표 없음, 시간 일관성 지표가 **"전부 흐리게/프레임 정지"라는 자명한 해에 최고점**을 줌 | §5.1 탐지 지표, §5.3 깨끗 영역 보호, §5.4 방어 지표 G1–G4 신설 |
| F-12 | 중대 | `EVALS.md` §4.5 | 사람 검토가 **"대표 구간 3개 선택"** — 체리피킹 가능. 만든 사람이 좋아 보이는 곳을 고를 수 있음 | §6 무작위·계층 추출 + blind A/B + 거부권 규칙으로 대체 |
| F-13 | 중대 | 없음(누락) | **재구성 안전 게이트 부재.** 의도적 비식별 처리(얼굴·신분증·보호 텍스트)를 자동 재구성할 수 있는 설계 | `ReconstructionPolicy/v1` 신설 (ADR-0022), `PRODUCT_SPEC.md` N13 |
| F-14 | 중대 | ADR-0018 | **"manifest로 재현성을 보장한다"는 거짓 주장.** GPU 비결정 경로에서 성립하지 않음 | 재현성 3등급(T1/T2/T3)으로 분리, manifest는 T3만 보증 |
| F-15 | 중대 | `AGENTS.md` R1 | **거버넌스 자기모순** — "병합하지 않는다"가 주체 구분 없이 적혀 사람 오너의 병합까지 금지하는 것처럼 읽힘 | §0.1 주체별 권한표 신설, ADR-0009 정정 |
| F-16 | 중대 | `AGENTS.md` §6 vs `TASK-001.md` §4.2 | **규칙 충돌** — "STATUS.md를 갱신하라"와 "지정 파일만 수정하라"가 동시에 요구됨. 규칙을 지키면 규칙을 어기게 됨 | §6.1 "항상 허용되는 기록 행위" 예외 신설 |
| F-17 | 중대 | `docs/tasks/` | **부트스트랩 공백** — 최초 문서 작업에 TASK 파일이 없음. 다음 에이전트가 예외를 원용할 수 있음 | `TASK-000.md` 소급 기록 + `AGENTS.md` §6.2 |
| F-18 | 중대 | 전 문서 | **용어 정책이 식별자에 미적용** — 문서는 "재구성", 코드는 `restore`/`RestoreAdapter` | `reconstruct`/`ReconstructionAdapter`로 통일 (ADR-0026) |
| F-19 | 중대 | `PRODUCT_SPEC.md` N12 | **틀린 논리** — "채점 피드백이 없어 학습 신호가 존재하지 않는다". 채점 신호 없이도 학습은 원리적으로 가능 | §6.3에서 근거를 범위·데이터·연산·일정 제약으로 재작성 |
| F-20 | 중대 | `DECISIONS.md` U-05 | **모델 라이선스 검토가 Phase 4로 미뤄짐.** 재배포 불가 모델로 파이프라인을 만든 뒤 알게 되면 재작업 | ADR-0019 신설 — 라이선스는 모델 후보 선정 시(U-22), 가격만 Phase 4 |
| F-21 | 경미 | `ARCHITECTURE.md` A1 | "두 파이프라인은 `ingest`와 `storage`만 공유" → 평가 하네스·시간축까지 중복 구현하라는 뜻으로 읽힘 | A1 재진술 + ADR-0025 (도메인 산출물 독립 / 횡단 기반 공유) |
| F-22 | 경미 | `README.md` | 에이전트 읽기 순서에서 `AGENTS.md`가 **마지막**에 위치 | `AGENTS.md` §0.2 읽기 순서 신설, 모든 진입 문서에서 첫 번째로 이동 |

---

## 3. 반영하지 않은 것 / 미해결로 남긴 것

**"반영했다"와 "답을 안다"는 다릅니다.** 아래는 구조만 만들고 값은 비워 둔 항목입니다.
추측으로 채우지 않았습니다 (`AGENTS.md` R5).

| 항목 | 왜 비워 두었나 | 등록 ID |
|---|---|---|
| dev/test 분할 비율, 최소 표본 수 | 코퍼스 규모를 모름 | U-26 |
| 최소 실질 효과 크기 | 기준선 분산을 측정해야 함 | U-27 |
| 안전 게이트 기본 정책 수준 | 법적·윤리적 판단 포함. 오너 결정 필요 | U-28 |
| 재현성 T2 허용오차 | 기준선 측정 후 | U-29 |
| 지표 임계값 `T_match`·`T_sync`·`T_sw`·`T_px` | 측정 전 확정 불가 | U-18, U-20 |
| `norm-v1` 정규화 규칙 내용 | 언어 목록 확정 후 | U-19 |
| 배점 비중 | **오너만 답할 수 있음** | U-24 |

---

## 4. 리뷰어에게 남기는 확인 요청 (다음 독립 검토용)

**다음 검토 주체: 새 독립 Claude Code 세션** (작성자 주 세션이 아닌 세션 — `AGENTS.md` §3.1).
이 반영이 충분한지 판단하려면 다음을 확인해야 합니다.

1. **F-01이 실제로 해소되었는가** — 문서 어디에도 "시각 재구성은 버려도 된다"로 읽히는 문장이 남아 있지 않은가
2. **F-05의 계산 규약이 구현 가능한 수준인가** — 두 사람이 같은 숫자를 얻을 수 있는가
3. **F-04 `ReferenceBundle`이 §4·§5의 모든 지표를 실제로 뒷받침하는가** — 빠진 정답 종류가 있는가
4. **F-11 방어 지표 G1–G4가 자명한 해를 실제로 막는가** — 우회 경로가 있는가
5. **새로 만든 ADR-0019~0026의 상태 라벨이 정직한가** — 승인됨으로 표시된 것이 정말 오너 승인 사항인가

---

## 5. 인계 메모

- 이 리뷰의 지적은 [`../tasks/TASK-000.md`](../tasks/TASK-000.md)의 산출물 전반에 반영되었습니다.
- 반영 커밋은 PR #1의 후속 커밋이며, **PR #1은 병합되지 않았습니다.**
- 다음 단계는 [`../../STATUS.md`](../../STATUS.md) §4를 보십시오.

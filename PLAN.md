# PLAN.md — 단계별 제품 전략

`media-clarity-studio`의 로드맵입니다. **각 단계는 앞 단계의 검증 위에서만 시작**합니다.

마지막 갱신: 2026-08-09

---

## 전략 요약

```
Phase 0  기반 문서            ← 지금 여기
   ↓  (사람 오너가 미해결 항목 일부 확정)
Phase 1  자막 품질 + 평가 기반   ← 가장 중요. 여기서 실패하면 나머지는 의미 없음
   ↓  (자막 파이프라인이 측정 가능해진 뒤)
Phase 2  영상 재구성 (독립 테스트 가능한 별도 파이프라인)
   ↓
Phase 3  제품 UI + 로컬 GPU 최적화
   ↓
Phase 4  상용화 + 개인 클라우드 내보내기
```

**핵심 원칙 세 가지**

1. **평가가 기능보다 먼저다.** 좋아졌는지 잴 수 없으면 좋아진 게 아닙니다.
   채점 원본이 비공개이므로, 우리가 만든 **합성 열화(synthetic degradation)** 기준이
   유일한 신뢰 가능한 신호입니다 ([`docs/EVALS.md`](docs/EVALS.md)).
2. **자막과 영상 재구성은 서로를 기다리지 않는다.** 두 파이프라인은 독립적으로 실행·평가됩니다.
   자막이 막혀도 영상 작업이 멈추지 않고, 그 반대도 마찬가지입니다.
3. **되돌릴 수 없는 결정은 최대한 늦춘다.** 모델·프레임워크·OS·GPU 벤더 선택은
   요구사항이 측정으로 확인된 뒤에 합니다.

---

## Phase 0 — Foundation (현재)

**목표:** 사람과 두 AI 에이전트가 같은 전제 위에서 병렬 작업할 수 있는 문서 기반 확보.

산출물:

- `README.md`, `AGENTS.md`, `CLAUDE.md`, `PLAN.md`, `STATUS.md`
- `docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/EVALS.md`, `docs/DECISIONS.md`
- `docs/tasks/TASK-001.md`

완료 조건:

- [ ] 문서 간 모순 없음 (교차 검토 완료)
- [ ] 기능 코드·의존성·모델·CI·비밀정보 **0건**
- [ ] 모듈 경계와 인터페이스 계약이 글로 정의됨
- [ ] 합성 열화 평가 전략이 문서화됨
- [ ] 미해결 항목이 `docs/DECISIONS.md`에 명시적으로 나열됨
- [ ] Codex의 독립 리뷰(TASK-001) 완료

**Phase 0 → 1 진입 조건:** TASK-001 리뷰가 끝나고, 사람 오너가
`docs/DECISIONS.md`의 **차단성(blocking) 미해결 항목**에 답을 준 상태.

---

## Phase 1 — 자막 품질 및 평가 기반

**왜 먼저인가:** 과제의 채점 대상이 자막이고, 자막은 텍스트라서 **정량 평가가 가능**합니다.
영상 재구성은 "좋아 보인다"의 주관성이 크지만, 자막은 WER·타임스탬프 오차로 잴 수 있습니다.
측정 가능한 쪽을 먼저 세워야 나중에 주관적인 쪽을 판단할 기준이 생깁니다.

**1a. 평가 하네스 먼저 (기능보다 앞)**

- 깨끗한 seed 코퍼스 + 확정된 정답(ground truth) 확보 방법 결정
- 합성 열화 레시피 구현 (잡음, 겹침, 무음, 대역 축소, 코덱 열화, 다국어 스플라이싱)
- 지표 계산기: WER/CER, 타임스탬프 정렬 오차, 무음 구간 환각(hallucination) 검출,
  언어 식별 정확도, 자막 포맷 규칙 검사
- 고정 시드 기반 재현 가능한 리포트 출력

**1b. 최소 자막 파이프라인**

- `ingest` → `audio` → `asr` → `subtitle` 경로의 **가장 단순한 동작 버전**
- 목표는 품질이 아니라 **끝에서 끝까지 흐르는 것**과 **평가 하네스에 물리는 것**

**1c. 어려운 입력 대응 (측정 기반 반복)**

- 겹치는 발화, 긴 무음, 다국어 전환, 저품질 오디오 각각에 대해
  **가설 → 변경 → 합성 열화 세트로 측정 → 채택/폐기**

완료 조건:

- [ ] 임의의 입력 영상에 대해 자막 파일이 생성됨
- [ ] 합성 열화 세트에 대한 점수 리포트가 재현 가능하게 생성됨
- [ ] 열화 강도별 품질 저하 곡선을 볼 수 있음
- [ ] 무음 구간 환각률이 지표로 측정됨
- [ ] 결과가 채점 원본 없이도 개선 여부를 판단할 수 있는 근거를 제공함

**차단 요인:** 정답이 확정된 seed 코퍼스를 어떻게 구할 것인가 — `docs/DECISIONS.md` **미해결 U-06**.

---

## Phase 2 — 영상 재구성 (별도 파이프라인)

**독립성 요구:** 이 파이프라인은 자막 파이프라인 없이 **단독 실행·단독 평가**가 가능해야 합니다.
공유하는 것은 `ingest`(미디어 읽기)와 `storage`(산출물 저장)뿐입니다.

- 열화 영역 탐지 (blur / mosaic / 저해상도 구간 식별)
- 영역별 재구성 시도 — 항상 **추정치**로 취급
- 시간적 일관성(temporal consistency) 확보: 프레임 간 깜빡임·정체성 흔들림 억제
- 인공물(artifact) 및 환각 탐지 — 없던 디테일이 생겼는지 검사
- 원본을 아는 합성 열화에서는 full-reference 지표(PSNR/SSIM/LPIPS),
  실제 입력에서는 no-reference 지표 + 사람 검토

완료 조건:

- [ ] 합성 열화 → 재구성 → 지표 산출이 자동으로 돌아감
- [ ] 시간적 일관성 지표가 존재함
- [ ] 인공물 탐지가 명백한 환각을 잡아냄
- [ ] 모든 산출물이 **추정치**로 라벨링되어 저장됨 (`AGENTS.md` §1)
- [ ] 사람 검토 루브릭이 정의되고 최소 1회 수행됨

**의도적으로 하지 않는 것:** "모자이크 완전 제거" 같은 목표. 그것은 달성 불가능한 주장입니다.

---

## Phase 3 — 제품 UI 및 로컬 GPU 최적화

- 로컬 데스크톱 UI: 파일 넣기 → 진행 상황 → 결과 확인 → 자막 수정 → 내보내기
- 자막 수동 교정 편집기 (자동 결과는 초안이라는 전제)
- 재구성 결과 **원본/추정 비교 뷰** — 사용자가 차이를 직접 판단할 수 있게
- GPU 가속: 벤더 중립 추상화 위에서 실측 후 최적화
- 파일 정리(file organization) 규칙 및 프로젝트 라이브러리
- 배치 처리, 작업 재개(resume), 캐시

**전제:** Phase 1–2에서 실제 자원 사용량을 측정한 뒤에야 UI 프레임워크와 GPU 백엔드를 고릅니다.
지금 고르면 근거 없는 선택이 됩니다 (`docs/DECISIONS.md` U-02, U-03).

---

## Phase 4 — 상용화 및 개인 클라우드 내보내기

- 라이선스·가격 정책 결정
- 개인 클라우드 내보내기(선택 기능, 기본은 로컬 유지)
- 자동 업데이트, 텔레메트리(옵트인 원칙), 지원 채널
- 모델 재배포 라이선스 검토 — **법적 검토 필요, 지금 판단하지 않음**

**원칙:** 클라우드는 **선택 기능**입니다. 로컬 전용으로도 제품이 완결되어야 합니다.
사용자 미디어를 기본값으로 외부에 보내지 않습니다.

---

## 병렬 작업 가능 지점

| 시점 | Claude 쪽 | Codex 쪽 |
|---|---|---|
| Phase 0 | 문서 기반 작성 | 독립 리뷰 (TASK-001) |
| Phase 1 | 평가 하네스 설계 | 자막 파이프라인 골격 (또는 교차) |
| Phase 2 | 재구성 파이프라인 | 재구성 평가·인공물 탐지 |

한 TASK의 Owner와 Reviewer는 항상 다릅니다 (`AGENTS.md` §3).

---

## 이 계획이 틀릴 수 있는 지점

정직하게 적어 둡니다.

- **Phase 1이 예상보다 오래 걸릴 수 있습니다.** 겹치는 발화와 다국어 전환은 난제입니다.
  과제 기한이 촉박하면 Phase 2를 미루고 자막에 집중하는 것이 옳습니다.
- **합성 열화가 실제 채점 입력과 다를 수 있습니다.** 이건 근본적 위험이며 완전히 없앨 수 없습니다.
  대응은 "레시피를 다양하게, 강도를 넓게" 뿐입니다 ([`docs/EVALS.md`](docs/EVALS.md)).
- **로컬 GPU만으로 목표 품질에 못 미칠 수 있습니다.** 그 경우 품질/속도 트레이드오프를
  사용자가 고르게 하는 것이 현실적입니다.

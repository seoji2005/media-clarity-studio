# PLAN.md — 단계별 제품 전략

`media-clarity-studio`의 로드맵입니다.

마지막 갱신: 2026-09-03 (TASK-032 3-ASR Work-CPU screening 계약 checkpoint)
상태: **제안됨 (Proposed) 유지** — PR #5 병합은 계획 기준선 작업을 완료했지만,
제안됨 ADR·실행 순서·미해결 U-XX를 자동 승인하거나 해결하지 않습니다

---

## 0. 먼저 — 두 개의 범위

이 로드맵은 **상용 제품(B 범위)의 착수 순서**입니다.
**대학 과제(A 범위)의 필수 산출물과 혼동하지 마십시오** ([`docs/PRODUCT_SPEC.md`](docs/PRODUCT_SPEC.md) §2).

| | 과제 범위 (A) | 이 로드맵 (B) |
|---|---|---|
| 자막 | **필수 — 채점 대상은 번역 자막** (U-08) | Phase 1 |
| 시각 재구성 | **필수 — 생략 불가** | Phase 2 |
| 배점 비중 | **모름 (U-24)** | 해당 없음 |
| 시간 예산 | **약 2개월 · 품질 우선** (U-11). 정확한 날짜 **미확정** | 해당 없음 |

> **Phase 번호는 착수 순서이지 중요도 순위가 아닙니다.**
> "Phase 2니까 나중에 해도 되고, 급하면 버려도 된다"는 해석은 **틀렸습니다.**
> 과제는 두 산출물을 모두 요구합니다 (AS-1). 범위 축소는 사람 오너만 결정합니다 (AS-4).

---

## 1. 전략 요약

```
Phase 0  기반 문서            ← 완료 (main에 병합됨)
   ↓
Phase 1  자막 + 평가 기반 (+ 시각 도메인의 공통 기반)   ← 지금 여기 (Phase 1a 진입)
   ↓
Phase 2  시각 재구성 (독립 산출물, 공통 기반 재사용)
   ↓
Phase 3  제품 UI + 로컬 GPU 최적화
   ↓
Phase 4  상용화 + 개인 클라우드 내보내기
```

**핵심 원칙 세 가지**

1. **평가가 기능보다 먼저다.** 좋아졌는지 잴 수 없으면 좋아진 게 아닙니다.
   단, 합성 열화는 **주된 신호이지 유일한 신호가 아닙니다** ([`docs/EVALS.md`](docs/EVALS.md) §1.1).
2. **도메인 산출물은 독립, 기반은 공유.** 자막과 시각 결과는 서로를 입력으로 쓰지 않지만,
   `ingest`·`storage`·`orchestrator`·`ReferenceBundle`·시간축 계약은 함께 씁니다.
3. **되돌릴 수 없는 결정은 최대한 늦춘다.** 모델·프레임워크·OS·GPU 벤더 선택은
   요구사항이 측정으로 확인된 뒤에 합니다.

**운영 원칙 — 약 2개월 · 품질 우선** (U-11, 2026-08-09 오너 답변)

- **속도보다 품질을 우선합니다.** 시간 예산은 **약 2개월**입니다.
- **정확한 제출 날짜는 아직 확정되지 않았습니다.** 이 로드맵은 **날짜로 고정된 마일스톤을
  두지 않습니다.** 정확한 날짜가 확정되면 그때 정밀 일정을 세웁니다 (R5).
- **품질 우선은 무제한 범위 확장이 아닙니다.** 과제 필수 산출물(번역 자막·시각 재구성)이
  **둘 다** 완성되는 것이 먼저입니다 (AS-1). 필수 산출물을 못 끝내면서 한쪽을 깊게 파는 것은
  품질 우선이 아닙니다.
- **품질 우선은 검증 생략이 아닙니다.** 원칙 1(평가가 기능보다 먼저)은 그대로이며,
  독립 리뷰 기준([`AGENTS.md`](AGENTS.md) §3.2)도 완화되지 않습니다.

---

## 2. Phase 0 — Foundation (**완료 — `main`에 병합됨**)

**목표:** 사람과 AI 세션들이 같은 전제 위에서 병렬 작업할 수 있는 문서 기반 확보.
누가 무엇을 맡는지는 [`AGENTS.md`](AGENTS.md) §3의 현재 운영 구조를 따릅니다 (ADR-0030).

산출물: `README.md`, `AGENTS.md`, `CLAUDE.md`, `PLAN.md`, `STATUS.md`,
`docs/PRODUCT_SPEC.md`, `docs/ARCHITECTURE.md`, `docs/EVALS.md`, `docs/DECISIONS.md`,
`docs/tasks/TASK-000.md`, `docs/tasks/TASK-001.md`, `docs/tasks/TASK-002.md`,
`docs/tasks/TASK-007.md`, `docs/reviews/REVIEW-001.md`

완료 조건:

- [x] 문서 기반 초안 작성
- [x] 독립 리뷰 1회 수행 (REVIEW-001)
- [x] 리뷰 지적 사항 반영
- [x] 운영 구조 확정 및 문서 반영 (TASK-007, ADR-0027)
- [x] Phase 0 병합 시 기능 코드·의존성·모델·CI·비밀정보 **0건**
- [ ] 문서 간 모순 없음 (독립 세션의 교차 검토)
- [ ] 미해결 항목이 `docs/DECISIONS.md`에 명시적으로 나열됨

**Phase 0 → 1 진입 조건 — 현재 상태**

| 조건 | 상태 |
|---|---|
| 리뷰 지적 반영 완료 | **완료** — PR #1 (REVIEW-001~004) |
| 운영 구조 전환 반영 | **완료** — PR #1 (TASK-007) |
| **U-08 (원어/번역) 답변** | **완료 (2026-08-09)** — **번역 자막** ([`docs/DECISIONS.md`](docs/DECISIONS.md) §4.1.1) |
| **U-11 (기한) 답변** | **부분 완료 (2026-08-09)** — **약 2개월·품질 우선.** 정확한 날짜 **미확정** (§4.1.2) |
| seed 코퍼스 조사·선택 | **완료** — TASK-003·TASK-026 (PR #21·#23) |

> **Phase 1 계획 착수 차단은 해제되었습니다.** U-08·U-11이 막던 것은 "Phase 1 계획 착수"였고,
> 2026-08-09 오너 답변으로 그 차단이 풀렸습니다.
>
> **그러나 정밀 일정 수립은 아직 막혀 있습니다.** "약 2개월"은 **운영상 시간 예산**이며
> **확정된 캘린더 마감일이 아닙니다.** 마일스톤·버퍼를 **날짜로 고정**하려면 정확한 제출일이
> 필요합니다. **임의의 날짜를 만들어 적지 않습니다** (R5).
>
> U-31은 2026-08-22 사람 제품 오너 답변으로 **한국어(BCP-47 `ko`)** 로 해소되었습니다.

> U-06은 TASK-026에서 **해소됐습니다.** 선택 조합은 Common Voice 26 일본어,
> LibriSpeech dev/test, deterministic 합성 코드스위칭이며 실제 다운로드·cache 생성은 별도 Gate H입니다
> ([`docs/EVALS.md`](docs/EVALS.md) §2.5).

---

## 3. Phase 1 — 자막 + 평가 기반 + 공통 인프라

**왜 먼저인가:** 자막은 정량 평가 체계를 세우기 쉽고, 그 과정에서 만드는
평가 하네스·`ReferenceBundle`·`Timebase`/`TimeMapping`·`ArtifactRef`를
**시각 도메인이 그대로 재사용**합니다. 즉 자막 먼저 = 시각 재구성의 기반 공사이기도 합니다.

이것은 **순서 제안이지 중요도 판정이 아닙니다** (ADR-0004, 제안됨).

### 1.0 채점 대상은 **번역 자막**입니다 (U-08)

**Phase 1 자막 경로에 번역 단계가 포함됩니다.** 원문 전사만으로는 채점 정답에 대응할 수 없습니다
([`docs/PRODUCT_SPEC.md`](docs/PRODUCT_SPEC.md) §2.0).

```
ingest → audio → asr ──▶ 원문 transcript      (채점 대상 아님, 보존)
                          │
                          ▼
                      translate ──▶ 번역 subtitle  (★ 채점 대상)
                          │
                          ▼
                       subtitle (분할·정렬·형식화) → 자막 파일
```

**평가는 두 축으로 분리합니다.**

| 축 | 무엇을 재는가 | 왜 분리하는가 |
|---|---|---|
| **원문 ASR 평가** | 원문 transcript의 인식 정확도 (WER/CER, 무음 환각, 겹침, 언어 식별) | 인식 오류인지 번역 오류인지 **출처를 구분**하기 위해 |
| **번역 자막 평가** | 번역 subtitle의 품질 (채점 대상) | 채점 대상을 직접 재기 위해 |

> **두 축을 하나의 숫자로 합치지 않습니다** (ADR-0015).
> ASR 지표만 좋고 번역이 나쁘면 채점은 나쁩니다. 번역 지표만 보면 원인을 모릅니다.
>
> **번역 대상 언어는 한국어(`ko`)입니다 (U-31, 2026-08-22 답변).** 번역 평가 지표와 실행 계약은
> **TASK-005에서 확정됐으며**, 한국어 정규화·CPS 규칙(U-18·U-19)은 아직 미정입니다.
> **이 절은 번역 모델·API·공급자를 고르지 않습니다.** TASK-031·ADR-0031이 A-min calibration 후보군만
> 별도로 고정했으며 기본 모델·공급자 채택은 측정 뒤까지 U-22에 남습니다.

### 1a. 평가 하네스와 공통 계약 (기능보다 앞)

- seed 코퍼스 **조사·오너 승인 완료** (U-06); 실제 확보는 별도 Gate H
- `ReferenceBundle/v1` 구체화, dev/frozen-test 분할 확정 및 누출 방지 (EVALS §2)
- **원문 축과 번역 축의 정답을 어떻게 담을지 확정** (U-08 반영, TASK-006)
- 공통 계약 확정: `ArtifactRef`, `Timebase`, `TimeMapping`, `RegionMask`
- 열화 레시피 구현 — **시간 변경 레시피는 `TimeMapping` 필수 반환**
- 지표 계산기 + 계산 규약(EVALS §4) 구현
- 통계 처리: 군집 부트스트랩, 신뢰구간, 최소 실질 효과 (EVALS §7)
- **공용 실행 기반:** content-addressed artifact, stage fingerprint/cache, checkpoint, 중단 후 재개 (TASK-028 완료·PR #36 병합)

### 1b. 최소 자막 파이프라인

- `ingest` → `audio` → `asr` → **`translate`** → `subtitle` 경로의 가장 단순한 동작 버전
- 목표는 품질이 아니라 **끝에서 끝까지 흐르는 것**과 **평가 하네스에 물리는 것**
- **원문 transcript와 번역 subtitle을 둘 다 산출·보존** (§1.0)
- 어댑터 능력 보고(`AdapterCapabilityReport`)와 대체 동작 구현

### 1c. 어려운 입력 대응 (측정 기반 반복)

- 겹치는 발화, 긴 무음, 다국어 전환, 저품질 오디오 각각에 대해
  **가설 → 변경 → dev 세트로 측정 → 신뢰구간·최소 효과 확인 → 채택/폐기**

### 1d. Phase 1a 첫 실행 순서 **(전체 후속 순서는 제안 유지; TASK-028은 완료됨)**

> **상태: 전체 후속 순서는 제안됨 유지.** TASK-012 완료와 PR #5 병합만으로 제안됨 ADR과
> 모든 후속 노드를 자동 승인하지 않습니다. TASK-028 계약은 PR #34, 구현은 REVIEW-022 승인 뒤
> PR #36으로 병합돼 완료됐습니다. TASK-003·TASK-005·TASK-006과 U-06 선택은 완료됐고
> U-31도 해소됐습니다. 다음 기능은 별도 TASK 계약과 제품 오너 승인 전에는 착수하지 않습니다.
>
> **승인된 한정 예외:** 2026-08-22 사람 제품 오너는 Gate S 합성 기술 게이트 승인에 따라
> 외부 코퍼스·모델·네트워크가 없는 synthetic media plumbing slice를 별도로 허용했습니다.
> TASK-022·TASK-023은 독립 Gate H 승인 후 PR #16으로 병합되었습니다. 이 예외는 아래의
> 제안 실행 순서, Gate E, U-06·U-07·U-22, 실제 ASR·번역·외부 서비스 선택을 승인하거나
> 해소하지 않습니다. U-31은 이 예외와 별개로 2026-08-22 사람 제품 오너가 해소했습니다.

**보수적 그래프 — 선행 조건은 한 가지로만 해석됩니다.**

```
U-31 완료  번역 대상 언어 = 한국어(`ko`) — 2026-08-22 사람 제품 오너 답변

TASK-003  seed 코퍼스·라이선스·합성 데이터 대안 조사 + 비교표 작성 — 완료
   │
   ▼
(사람) U-06 선택 — TASK-026에서 완료
   │
   ▼
TASK-005  평가 하네스 설계 명세 — 완료 (PR #25)
   │
   ▼
TASK-006  ReferenceBundle/v1·평가 실행 계약과 schema/validator/fixture — 완료 (PR #28)
   │
   ▼
TASK-028  content-addressed artifact/cache/resume runtime — 완료 (PR #36)
   │
   ▼
TASK-029  자막 spine 기계 계약·schema·validator·fixture — 완료 (PR #45)
   │
   ▼
TASK-031  고정 10분 A-min 로컬 자막 calibration vertical slice — exact synthetic 8-cell/12-stage validation 병합 완료
   │
   ▼
TASK-032  공개·허가·합성 frozen pack 3-ASR Work-CPU screening — 계약 checkpoint
   │
   ▼
TASK-031  screening 상위 2종만 별도 후보 amendment/review 뒤 local 2×2로 복귀
```

| 노드 | 성격 | **선행 (이 표가 정답)** | 현재 상태 |
|---|---|---|---|
| **U-31** | 제품 오너 결정 | 없음 | **완료 — 한국어(`ko`)** |
| **TASK-003** | 에이전트 조사 (기존 의미 유지) | 없음 | **완료 — PR #21** |
| **(사람) U-06 선택** | 제품 오너 결정 | TASK-003 결과 | **완료 — TASK-026 / PR #23** |
| **TASK-005** | 평가 하네스 설계 명세 (기존 의미 유지) | TASK-003 · U-06 선택 | **Done — PR #25** |
| **TASK-006** | `ReferenceBundle/v1`·평가 실행 계약과 schema/validator/fixture (기존 의미 유지) | **TASK-005** | **Done — PR #28** |
| **TASK-028** | 공용 storage·orchestrator 실행 기반 | TASK-006 완료 | **Done — REVIEW-022 승인, PR #36 병합** |
| **TASK-029** | 자막 spine 기계 계약과 기계 정본 구현 | TASK-028 완료와 별도 TASK 계약 | **Done — Gate H 승인, PR #45 병합 (`6f94705598c1ef57a4d25682938cbcbbaf044732`)** |
| **TASK-031** | U-22 A-min 실제 자막 vertical slice와 calibration | TASK-029 완료, 제품 오너 A-min 결정, 별도 dependency/model/network gate | **In progress — exact synthetic 8-cell/12-stage validation은 Gate H 승인 뒤 PR #59로 병합. 실제 evidence 축과 target Windows 실측은 후속** |
| **TASK-032** | 공개·합성 frozen pack 3-ASR Work-CPU screening | PR #60 상태 정합화 merge, 제품 오너 Work-CPU·비민감 자료·비용 0 gate | **In progress — 별도 Gate H 계약 checkpoint. TASK-031 8/12 closed set은 불변** |

**왜 이렇게 보수적으로 두는가**

| 판단 | 근거 |
|---|---|
| **TASK-006의 선행을 TASK-005로 둔다** | `ReferenceBundle/v1`은 **어떤 지표를 계산할지**가 정해져야 정답 필드를 고정할 수 있습니다. TASK-005보다 먼저 시작하면 스키마 재작업 위험이 있습니다. **병행 가능 여부는 아직 검증되지 않았으므로 직렬로 둡니다** |
| **U-31은 TASK-005 차단에서 제거한다** | 사람 제품 오너가 대상 언어를 한국어(`ko`)로 확정했습니다. TASK-003과 U-06 선택도 완료되어 TASK-005 선행 차단은 없습니다 |
| **TASK-028을 모델보다 먼저 둔다** | 긴 ASR·번역·평가 stage를 붙이기 전에 원본 불변·content hash·cache 무효화·checkpoint·resume을 공용화해야 데이터 손상과 전체 재실행 위험을 줄일 수 있습니다. 정확도 모델 선택은 동일 corpus 직접 비교 뒤에 합니다 |

> **각 TASK는 착수 전에 자기 TASK 파일을 먼저 만듭니다** (`AGENTS.md` §6.2).
> **TASK-003·005·006은 예약된 번호이며 의미를 바꾸지 않았습니다.**
> **TASK-028 계약은 PR #34, 구현은 REVIEW-022 승인 뒤 PR #36으로 병합됐습니다.**
> **TASK-029 계약·구현은 Gate H 검토와 제품 오너의 exact-HEAD 승인 뒤 PR #45로 병합됐습니다.**
> 다음 기능은 TASK-031로 고정됐습니다. 세부 실행 정본은 [`TASK-031`](docs/tasks/TASK-031.md)과
> ADR-0031이며, 계약 checkpoint는 Gate H 승인 뒤 PR #50으로 병합됐습니다. 2026-09-01 제품 오너가
> dependency/model/network gate를 승인했고, fail-closed preflight 기반은 Gate H 승인 뒤 PR #52로
> 병합됐습니다. hardware-independent offline evidence core도 Gate H 승인 뒤 PR #54로 병합됐습니다.
> PR #54 병합 상태 정합화는 PR #55로, manifest/report evidence spine은 G56-01 remediation과 Gate H 승인 뒤
> PR #56으로 병합됐습니다. exact synthetic 8-cell/12-stage fail-closed validation은 Gate H 승인 뒤
> PR #59로 병합됐고 상태 정합화는 PR #60으로 병합됐습니다. 별도 TASK-032는 공개·허가·합성 자료로
> 세 ASR 후보를 Work CPU에서 먼저 screen하고, 상위 2종만 별도 TASK-031 후보 amendment/review 뒤
> local 2×2에 올립니다. 유료 provider/GPU의 현재 지출 상한은 0이며, target Windows 접근 뒤
> hash lock·environment evidence·exact model receipt를 준비하고 실제 calibration으로 진행합니다.
> 위의 PR #16 합성 plumbing은
> 사람 오너가 승인한 좁은 예외이며 일반 ASR·번역·외부 코퍼스 구현 착수를 뜻하지 않습니다.
> **PR #34와 #36의 승인은 TASK-028에 한정됩니다.** 나머지 실행 순서와 후속 노드는 계속 제안 상태입니다.

### 1e. TASK-031 — 첫 실제 10분 자막 vertical slice

> **TASK-032 경계:** 3-ASR Work-CPU screening은 별도 evidence lineage다. 결과가 나오기 전과
> 별도 amendment/review 전에는 TASK-031의 ASR 2종, 8 logical runs/12 candidate stages를 바꾸지 않는다.

TASK-031은 새 추상 계약을 확장하지 않고 기존 spine·CAS·resume을 실제 모델에 연결한다. 고정 10분
pack에서 독립 ASR 2회, 동일 human-corrected source의 독립 번역 2회, 네 ASR×번역 end-to-end 조합을
실행한다. 한 조합 smoke는 선행 경로 점검일 뿐 완료가 아니다. UI·OCR/VLM·고급 diarization·remote
comparator·시각 재구성은 이 TASK에 섞지 않는다. 자막 slice 뒤에는 최소 시각 재구성 vertical slice를
다음 후보로 평가해 과제의 두 번째 필수 산출물을 늦게 발견하는 위험을 줄인다.

완료 조건:

- [ ] 임의의 입력 영상에서 **번역 자막 파일** 생성 (채점 대상)
- [ ] **원문 transcript도 함께 산출·보존됨** (§1.0)
- [ ] **원문 ASR 평가와 번역 자막 평가가 분리되어 보고됨** (§1.0)
- [ ] dev 세트 리포트가 재현 가능하게 생성 (재현성 등급 명시)
- [ ] 열화 강도별 저하 곡선 + 불확실성 구간
- [ ] 무음 환각률과 무음 오탐률이 **함께** 측정됨
- [ ] 미지원 지표가 "미지원"으로 보고됨 (0으로 채우지 않음)
- [ ] 공통 계약이 시각 도메인에서도 쓸 수 있는 상태로 확정됨

---

## 4. Phase 2 — 시각 재구성

**과제 필수 산출물입니다** (AS-1). 착수가 Phase 1 뒤일 뿐 선택 사항이 아닙니다.

**독립성 요구:** 시각 산출물은 자막 산출물을 입력으로 쓰지 않습니다.
반면 `ingest`·`storage`·`orchestrator`·평가 하네스·공통 계약은 **재사용**합니다.

- 열화 영역 탐지 — 이동 영역(`RegionMask`) 지원, **탐지 지표부터 측정** (EVALS §5.1)
- **`ReconstructionPolicy` 안전 게이트** — 이것 없이 재구성 단계를 만들지 않음
- 영역별 재구성 시도 — 항상 **추정치**로 취급
- 시간적 일관성 + **자명한 해 방어 지표** G1–G4 (EVALS §5.4)
- 인공물·환각 탐지, **깨끗 영역 보호 지표** (EVALS §5.3)
- 무작위·계층·blind A/B 사람 검토 (EVALS §6)

완료 조건:

- [ ] 열화 영역 탐지 지표가 측정됨
- [ ] 안전 게이트가 동작하고 판정이 기록됨
- [ ] 합성 열화 → 재구성 → 지표 산출이 자동으로 돌아감
- [ ] 시간적 일관성 지표가 **방어 지표와 함께** 보고됨
- [ ] 깨끗 영역이 훼손되지 않음이 수치로 확인됨
- [ ] 모든 산출물이 **추정치**로 라벨링됨 (`AGENTS.md` §1)
- [ ] blind A/B 검토가 절차대로 최소 1회 수행됨

**의도적으로 하지 않는 것:** "모자이크 완전 제거" 같은 목표. 달성 불가능한 주장입니다.

---

## 5. Phase 3 — 제품 UI 및 로컬 GPU 최적화

- 로컬 데스크톱 UI: 파일 넣기 → 진행 → 결과 확인 → 자막 수정 → 내보내기
- 자막 수동 교정 편집기 (`needs_review` 우선 표시)
- **원본/추정 비교 뷰** — 사용자가 차이를 직접 판단
- **안전 게이트 확인 UI** — 정책상 확인이 필요한 영역을 사용자에게 제시
- GPU 가속: 벤더 중립 추상화 위에서 실측 후 최적화
- 파일 정리 규칙, 프로젝트 라이브러리, 배치 처리, 재개, 캐시

**전제:** Phase 1–2에서 실제 자원 사용량을 측정한 뒤 UI 프레임워크와 GPU 백엔드를 고릅니다 (U-02, U-03).

---

## 6. Phase 4 — 상용화 및 개인 클라우드 내보내기

- **가격·수익 모델 결정** (U-05)
- 개인 클라우드 내보내기 (선택 기능, 기본은 로컬 유지) (U-14)
- 자동 업데이트, 텔레메트리(옵트인), 지원 채널

> **모델 라이선스·재배포 검토는 Phase 4가 아닙니다.**
> 모델 후보를 고르는 시점(U-22, Phase 1a 후반)에 **선정 기준의 일부**로 확인합니다.
> 재배포 불가 라이선스를 모르고 채택하면 나중에 파이프라인을 다시 만들어야 합니다 (ADR-0019).

---

## 7. 역할과 병렬 작업 경계

현재 역할·Claude escalation·동시성·검토·병합 규칙의 정본은
[`AGENTS.md`](AGENTS.md) §3~§4와 ADR-0030입니다. 이 로드맵은 역할을 재정의하지 않습니다.
작업은 모듈·파일 소유권이 겹치지 않을 때만 병렬화하며 제품 Phase 순서는 그대로 유지합니다.

---

## 8. 이 계획이 틀릴 수 있는 지점

- **Phase 1이 예상보다 오래 걸릴 수 있습니다.** 겹치는 발화와 문장 내 언어 전환은 난제입니다.
  일정이 부족하면 **각 도메인의 목표 수준을 낮추는 것**이 먼저이고,
  **한 도메인을 통째로 버리는 것은 사람 오너의 결정**입니다 (AS-4).
- **합성 열화가 실제 채점 입력과 다를 수 있습니다.** 근본 위험이며 제거할 수 없습니다.
  대응은 신호를 여럿 쓰는 것입니다 (EVALS §1.1).
- **로컬 GPU만으로 목표 품질에 못 미칠 수 있습니다.** 그 경우 품질/속도 트레이드오프를
  사용자가 고르게 하는 것이 현실적입니다.
- **U-24(배점)가 끝까지 공개되지 않을 수 있습니다.** 그때도 두 산출물을 동등하게 다룹니다.
  모르는 비중을 추측해서 한쪽을 버리지 않습니다.
